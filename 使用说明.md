# Codex Strip — Windows 任务状态条

双击 `CodexStrip.exe` 启动。需要保持 Codex 桌面应用开启；无需安装 Node，也无需另建 SSH 连接。

**界面放大与全屏**：设置 → 显示 → 界面倍率可选择 100%、125%、150%、175% 或 200%，默认 150%，会自动保存。顶部四角形按钮直接进入全屏；再次点击或按 F11 / Esc 退出，并恢复原来的窗口位置或停靠状态。全屏时覆盖当前显示器，适合 1920 × 450 等窄屏。全屏不会改变 Windows 的显示缩放。

## 休眠资源监控

保持 Codex 连接且没有进行中的任务、未读结果、等待回答、待确认、断连或错误状态时，静息计时开始。默认 5 分钟后自动切换为资源监控；一旦出现新任务或待处理事项，立即返回任务看板。点击资源视图任意位置也可返回，并重新开始静息计时。

在“设置 → 休眠监控”中可以开关自动切换、选择 1/3/5/10/30 分钟静息时间，自由显示、隐藏及调整 CPU、GPU、内存、网速、磁盘项目顺序。还可以选择网络适配器和速率单位、强调色、本地背景图片，以及背景遮罩深度。背景图片仅用于本软件资源视图，不更改 Windows 桌面壁纸；路径保存在本地 `data/settings.json`，图片不可读取时自动显示默认背景。

CPU、内存和网络使用 Windows 本机统计；磁盘读写使用 Windows 性能计数器。GPU 利用率由 Windows GPU Engine 计数器估算，可能随显卡驱动而偏差；计数器不可用时显示“—”。

## 外观（新版）

白天、夜晚和跟随系统三种外观。右上角太阳／月亮可立即切换；设置窗口可选择跟随 Windows。任务卡、设置、额度窗及滚动条使用同一套主题。设置即时生效并自动保存。

## 操作

- **屏幕停靠**：默认停靠当前显示器顶部，向 Windows 注册 AppBar 并保留工作区。设置页中的“屏幕停靠”区域可选择顶部、底部、左侧、右侧或浮动，选择显示器并调节停靠厚度。顶部／底部自动使用横屏布局，左右侧自动使用竖屏布局。锁定时不能拖动、最大化或直接拉伸；在停靠设置里调整，或先切换浮动。
- **工作区恢复**：收起到托盘或退出时释放工作区，重新显示时再次预留；从任意侧边切换浮动时，窗口按原尺寸约 78% 缩小，留在原停靠边附近，不跳回历史位置。最小尺寸和显示器工作区会限制缩放。普通最大化窗口会避让 AppBar，全屏视频、游戏或自行忽略 Windows 工作区的应用不能保证避让。
- **横竖转置**：点击顶栏双向箭头，浮动时按当前显示器可用工作区换算长边占屏比例，厚度不再按屏幕比例增宽。首次竖向宽度为 320 逻辑像素，首次横向高度为 286；按 Windows 显示器标识分别记忆横向高度和竖向宽度。浮动窗口拖动缩放结束时、停靠厚度调整时自动保存。Windows 缩放只换算物理像素。停靠时顶部与左侧互换、底部与右侧互换，厚度恢复对应方向的记忆值，同时更新预留工作区。极窄或极矮的屏幕以能容纳窗口为先，最小尺寸可能限制目标方向及比例。

- **自动横竖屏**：窗口高度大于宽度时切换竖屏；无需单独安装版本。竖屏任务卡纵向等高排列，数量和排序规则不变。额度／设置在顶部展开加高工具卡；预览把任务收拢到顶部。窗口内滚动、右键进出预览、同按钮开关和高亮规则均保留。最小宽度 320，窗口尺寸会自动保存。

- **点击任务卡片**：在 Codex 最近使用的主窗口中打开对应任务；该卡片立即移到最左边。
- **设置 → 展示数量**：1–12 个，默认 4 个。可筛选全部主机、本机或已连接的远端主机。
- **排序**：依据最近一轮对话开始时间、可识别的最新用户消息时间，以及从看板点击的时间。后台工具输出不会单独提升排序。它不追踪你只切换 Codex 标签、却没有发消息的操作。
- **点击右上角“周剩余”**：在窗口内展开额度面板。显示周额度剩余、下次重置时间、可用重置卡数量和每张卡的到期时间，最早到期的排前面。时间使用 Windows 本地时区。
- **预览**：右键任务卡打开阅读面板，在预览内再次右键，或右键同一任务卡即可关闭；悬停不触发。支持滚动、选择复制、标题／粗体／代码的基本排版；“打开线程”跳转至 Codex。左侧紧凑任务列表可切换预览。Esc 和 × 也可以关闭。
- **额度与设置**：以并排的加宽工具卡展开，原任务卡仍横向排列；再次点击对应的顶部按钮即可收起，按钮边框表示当前选中。两种工具卡互相切换，小尺寸下任务横向滚动、工具内容内部纵向滚动。
- **动画与边界**：展开或收起约 300 毫秒，所有内容始终位于软件窗口内，遵循 Windows 的关闭动画设置。
- **拖动顶栏**：移动到副屏。拖动边缘可缩放，双击顶栏最大化／还原，Esc 退出最大化。
- **置顶**：设置提供“不置顶”“窗口置顶”“始终置顶”三档。不置顶遵循普通窗口层级；窗口置顶位于普通窗口之上，检测到同屏前台全屏窗口时退让，退出全屏后恢复；始终置顶在检测到全屏时继续保持前置。检测间隔约 250 毫秒。独占全屏游戏无法保证覆盖；这些模式不会强迫全屏应用缩小画面。
- **F5**：立即刷新列表。已订阅线程通过桌面共享流推送正文和状态，界面每 250 毫秒合并一次变更；列表发现约每 5 秒一轮，额度约每分钟刷新。
- **收起与退出**：右上角 − 按钮收起到托盘，后台继续更新。左键点击托盘图标显示／隐藏；右键提供账户额度、设置和“退出 Codex Strip”。窗口位置、大小、展示数量、主机筛选、外观及看板点击记录保存在软件旁的 `data/settings.json`。

窄窗口会出现横向滚动条，避免中文卡片被挤得无法阅读。建议先在主屏尝试窗口尺寸，再决定副屏规格。

## 数据与限制

软件通过 Codex 自带的本机命名管道调用任务列表、状态、消息、额度和导航接口。它不会发送对话、消耗重置卡、修改 Codex 配置、启动模型请求或重新连接 SSH。

自动发现管道时，会读取本机 `.codex/session_index.jsonl` 的末尾，从中找到一个已有任务 ID，作为内部接口要求的路由上下文。不会读取登录令牌。若没有本地任务索引，请先在 Codex 中打开一个已有的本地任务。

线程发现结合工具返回的最近 50 条非固定任务、固定任务、手动关注映射，以及桌面全局状态中的最近 64 个线程 ID 和主机元数据。正文从 codex-ipc 共享流读取，复用 Codex 已建立的远端连接。只有当前桌面有对应会话所有者并提供快照的线程，才能收到这条共享流；不能保证覆盖全部历史线程。

“已完成”指最近一轮 Codex 回复结束，不代表它启动的训练进程结束；“未加载”也不代表训练停止。断连时保留现有卡片并显示“待同步”。

本版使用 Codex 内部接口，已针对桌面版 26.908.4834.0 / CLI 0.154.0-alpha.6.2 验证。升级后接口可能变化；无法读取时会显示断连或数据不可用。额度值未知时显示 —，不会当作 0%。

## 文件

- `CodexStrip.exe`：直接运行的软件。
- `CodexStrip.exe.config`：.NET Framework 4.8 配置，请保留在同一目录。
- `source/`：完整源代码；可运行 `source/build.ps1` 重建。

需要 Windows 的 .NET Framework 4.8。源码仅使用系统框架，无第三方运行库。

开发验证选项：`--self-test` 执行离线逻辑检查；`--demo` 打开明确标注的演示界面；`--probe` 只读验证实际连接并输出 `probe-result.json`。正常使用不需要这些参数。

设计参考来自 VoltAgent/awesome-design-md 的 Linear 和 Apple DESIGN.md，已适配 Windows 紧凑工具界面。`day.png` / `night.png` 以及对应设置、额度截图均为明确标注的演示数据。详见 DESIGN.md。


消息预览：优先读取桌面 conversation snapshot 和增量 patches，支持当前 turnHistory 存储结构。只展示助手可见消息，不展示内部推理或工具原始输出。新一轮暂无回复时保留最近回复；断线保留旧内容并标记待同步，重新连接或版本缺口时请求新快照。未接入共享流的线程继续使用工具接口及本地 sessions 日志作为补充。

异步提问：只将仍在进行的回合中带 questions 的异步消息显示为待回答；按实际回答中的 questionItemId 清除对应问题，部分回答保留其余问题。回合完成、中断或失败时清除等待标记，与桌面问答面板的生命周期一致。历史问题不会在新回合重新触发提醒。普通待处理提问和审批请求也会显示。
需要注意的状态使用大号粗体标签及强调边框：等待回答、需要确认、重连／待同步、模型容量不足。容量提示仅在接口返回匹配的实际错误信息时出现；不从普通对话内容推断。

最新回复已完成但尚未查看时，卡片使用绿色底和绿色边框，并显示“完成 · 未查看”。优先读取 Codex 蓝点实际使用的 electron-thread-read-state-v1 未读列表，每 2 秒独立核对一次，并接收 thread-read-state-changed 事件。打开结果使 Codex 标记已读后即可取消高亮，不需要发送新消息。无法明确区分账户或主机时不合并不同账户的数据；没有可用独立状态时回退到会话快照的未读值。

托盘模式：主窗口及附属窗口不显示在任务栏。Windows 决定托盘图标显示于通知区域还是“小箭头”的隐藏图标区；可在 Windows 任务栏设置中调整。首次启动仍显示看板，顶栏只保留 − 收起按钮，退出请使用托盘菜单。

同步显示：后台读取整轮任务数据，期间保留上一份完整内容，完成后统一更新。读取失败保留原消息并标注状态；卡片内容未变化时不重建控件。
已知限制：这是 Codex 内部桌面协议，当前读取 thread-stream-state-changed v11，后续升级可能需要适配。桌面未提供共享快照且 read_thread 返回空消息的线程仍会显示“消息暂不可读”。共享流更新速度取决于 Codex 的远端连接与桌面事件推送，不承诺固定的端到端延迟。





转置位置记忆：浮动窗口按显示器及横竖方向保存位置和完整尺寸，再转回来恢复原位置。屏幕工作区或缩放改变时进行边界适配；停靠窗口仍由对应屏幕边缘定位。

点击任务卡或预览中的打开线程：先恢复并激活 Codex 桌面主窗口，再导航到线程。按安装路径识别桌面进程，兼容进程名 ChatGPT；仅在用户点击时执行，不在同步时抢焦点。已验证真实 Codex 后台唤起和最小化恢复。

自动靠边：顶栏箭头靠边按钮，竖向选择当前显示器最近的左／右边，横向选择最近的上／下边，进入停靠并预留工作区。托盘展开后校验真实窗口位置，最小化先恢复窗口再重新定位。


停靠按钮为双向开关：已停靠时强调色高亮，点击恢复浮动；浮动时点击停靠到最近侧边。箭头显示当前停靠方向或预计方向，并随移动、缩放更新。
