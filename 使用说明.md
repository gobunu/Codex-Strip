# Codex Strip 使用说明

## 运行

解压便携包后，双击 `CodexStrip.exe`。请保留同目录的 `CodexStrip.exe.config`。软件需要 Windows、.NET Framework 4.8 和正在运行的 Codex 桌面应用。个人设置保存在软件目录下的 `data/settings.json`；更新程序时可保留该文件。

## 静息监控

保持 Codex 连接且没有进行中的任务、未读结果、等待回答或待确认事项时，开始静息计时。默认 5 分钟后切换到独立的资源监控页面。新任务或待办出现时，页面自动返回任务看板；点击静息页面任意位置也会立即返回。

CPU、GPU、内存默认使用单环：灰色表示剩余比例，彩色表示总占用；彩色部分以同色系的不同深浅表示主要程序及其他程序的占比。圆心显示总使用率，程序名称和占比列在圆环下方。网速与磁盘显示实时读写速率。

在“设置 → 休眠监控”中，可开关自动切换并选择 1、3、5、10 或 30 分钟静息时间。点击“自定义项目、壁纸和颜色”，可调整显示项目与顺序、圆环或条形样式、网络适配器、速率单位、强调色、背景图片和遮罩深度。背景图片只用于本软件，不会更改 Windows 桌面壁纸。图片无法读取时显示默认背景。

CPU 程序占比来自进程 CPU 时间；内存程序占比来自进程工作集。共享内存和系统缓存会使分程序之和与总占用不同。GPU 数据由 Windows GPU Engine 性能计数器估算，结果可能随驱动而偏差；计数器不可用时显示“—”。

## 看板与窗口

- 设置 → 显示 → 界面倍率：100%、125%、150%、175% 或 200%，默认 150%。该设置只改变软件界面，不改变 Windows 显示缩放。
- 顶部全屏按钮或 F11 可切换全屏；Esc 退出。退出后恢复先前的浮动位置或屏幕停靠。
- 点击任务卡可在 Codex 中打开对应任务；右键任务卡可查看消息预览。等待回答、需要确认和未读完成会以文字和颜色提示。
- 点击“周剩余”查看额度信息；点击设置按钮调整展示数量、主机、外观、置顶模式和屏幕停靠。
- 可停靠在显示器的上、下、左、右边，也可使用浮动窗口。停靠厚度在设置中调整；顶栏按钮可切换横竖布局。
- 右上角的收起按钮将软件放入系统托盘。托盘菜单可重新显示窗口或退出程序。

软件读取本机任务状态和系统资源信息，用于界面显示。它不会代替用户发送对话或使用额度。由于任务状态接入依赖 Codex 桌面应用的接口，Codex 更新后可能需要相应更新。

